node_modules/
dist/
.env
# state.json is persisted via GitHub Actions cache, not git commits.
state.json
state.local.json
*.local.json
*.log
# cached OFAC SDN list (regenerated from public source)
data/
